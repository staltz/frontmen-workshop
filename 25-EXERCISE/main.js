function bfs(root) {
  /**
   * Exercise: implement this function to return an
   * iterable that does breadth-first search over
   * all the elements under the given 'root'.
   *
   * Hint: FIFO.
   */
}

for (let elem of bfs(document.body)) {
  console.log(elem.id);
}
// Should output:
// body
// a
// b
// c
// d
// e
// f
// g
// h
// i
// j
